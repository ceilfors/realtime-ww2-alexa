service:
  name: realtime-ww2

plugins:
  - serverless-webpack

provider:
  name: aws
  runtime: nodejs6.10
  region: eu-west-1

functions:
  first:
    handler: handler.luckyNumber
    events:
    - alexaSkill

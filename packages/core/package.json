{
  "name": "@realtime-ww2-alexa/core",
  "version": "0.1.0",
  "description": "The core alexa skill",
  "scripts": {
    "pretest": "standard",
    "test": "mocha --compilers js:babel-core/register --require babel-polyfill test/{**,}/*.spec.js --color | bunyan -l fatal",
    "testDebug": "mocha --compilers js:babel-core/register --require babel-polyfill test/{**,}/*.spec.js --inspect-brk"
  },
  "author": "ceilfors",
  "license": "Apache-2.0",
  "private": true,
  "dependencies": {
    "ajv": "^5.2.2",
    "bunyan": "^1.8.12",
    "moment": "^2.18.1",
    "twitter": "^1.7.1"
  }
}
